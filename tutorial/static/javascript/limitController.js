// an attempt to make a "limit controller" to control 
// the maxItem variable of a bar chart.

function maxItemController() {

    function controller( chart ) {
	
    }

    return controller;

}
